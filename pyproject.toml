[project]
name = "inkpath-pipeline"
version = "0.1.0"
description = "GRBL 写字机 SVG 排版、G-code 后处理与 GUI 工具"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
inkplot = "plotter_tool.cli:main"
inkplot-gui = "plotter_tool.gui:launch"
